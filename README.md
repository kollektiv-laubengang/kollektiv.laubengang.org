# Kollektiv Laubengang

## Development

If you want easy dev server, just use browser sync

```bash
npx browser-sync -w
```

## Deployment

Everything on `main` will be automatically deployed via GitHub Actions.
